# 对话画布 开发日志

> 项目：conversation-canvas
> 仓库：https://github.com/kkry486/conversation-canvas
> 开发周期：2026年6月18日 — 6月19日
> 开发方式：AI 辅助开发（Claude Code）

---

## 第一阶段：项目规划与初始化（6月18日）

### 1.1 需求来源

用户提出将 AI 对话系统做成类似 ComfyUI 的节点图画布效果。

### 1.2 技术选型

| 决策项 | 选择 | 原因 |
|--------|------|------|
| 前端框架 | SvelteKit | 前后端一体，开发效率高 |
| 节点编辑器 | LiteGraph.js（npm 原版） | ComfyUI 同款引擎 |
| 后端 | SvelteKit API Routes | 无需额外服务 |
| AI API | OpenAI 兼容格式 | DeepSeek V4 原生支持 |

### 1.3 项目初始化

手动创建 SvelteKit 项目，14 个源文件，覆盖前端画布、节点定义、后端 API、全局样式。

---

## 第二阶段：核心功能开发（6月18日）

- LiteGraph 画布集成、深色主题
- 三种自定义节点：模型配置、对话输入、AI 回复
- SSE 流式后端 API
- 对话历史回溯（分叉核心）
- 回复滚轮滚动、文本复制、System Prompt

---

## 第三阶段：高级功能（6月18日）

- 画布缩略图（Minimap）
- 节点搜索
- 对比视图（5 次迭代）
- 多模型支持（7 个预设）
- 数据持久化（导出/导入 JSON）

---

## 第四阶段：Agent 化（6月18日晚 — 6月19日）

### 4.1 Agent 工具模块

创建 `src/lib/tools/index.js`，实现：

- **5 个工具定义**：read_file、write_file、list_directory、execute_command、search_in_files
- **工具执行器**：每个工具的实际执行逻辑，带路径安全检查
- **Agent 循环引擎**：`runAgentLoop()` 实现 LLM → tool_use → 执行 → 回传 → 继续的循环，最多 10 轮

### 4.2 Agent API 路由

创建 `src/routes/api/agent/+server.js`：

- 接收 chat 请求，启动 Agent 循环
- 通过 SSE 流式推送每一步（thinking、text_delta、tool_call、tool_result、done）
- 前端实时接收事件并创建对应节点

### 4.3 Agent 节点类型

新增 4 个节点类型：

| 节点 | 类型名 | 颜色 | 功能 |
|------|--------|------|------|
| 🧠 思考 | Agent/思考 | 紫色 | LLM 推理过程 |
| 🔧 工具调用 | Agent/工具调用 | 蓝色 | 调用的工具名 + 参数 |
| 📋 工具结果 | Agent/工具结果 | 绿/红 | 工具执行返回值 |
| 📄 文件产物 | Agent/文件产物 | 蓝色 | 创建的文件路径 + 打开按钮 |

### 4.4 文件产物节点

自定义绘制按钮（不用 LiteGraph 内置按钮），解决文字不居中问题：
- **▶ 打开文件**：调用系统默认程序打开文件
- **📂 打开文件夹**：在资源管理器中打开并选中文件
- 后端 `/api/open` 路由支持 `mode: 'file'` 和 `mode: 'folder'`

### 4.5 Agent 模式切换

工具栏新增 **🤖 Agent** 开关按钮，紫色高亮。点击切换对话/Agent 模式。

---

## 第五阶段：Tauri 启动器（6月19日）

### 5.1 Rust 环境搭建

- 安装 Rust 1.96.0（通过代理加速下载）
- 安装 create-tauri-app 4.7.3
- 安装 tauri-cli 2.11.3

### 5.2 Tauri 项目集成

在现有 SvelteKit 项目中集成 Tauri v2：

```
src-tauri/
├── Cargo.toml          # Rust 依赖：tauri、reqwest、tokio、serde_json
├── tauri.conf.json     # Tauri 配置：窗口尺寸、构建命令
├── build.rs            # 构建脚本
├── capabilities/main.json  # 权限配置
├── icons/icon.ico      # 最小占位图标
└── src/
    ├── main.rs         # Windows 子系统入口
    └── lib.rs          # 核心逻辑：chat、agent、open_file 命令
```

### 5.3 Rust 后端 API

将 Node.js 后端全部迁移到 Rust：

| 原 Node.js API | Rust 命令 | 说明 |
|---|---|---|
| `/api/chat` | `chat` | 调用 LLM API，返回完整回复 |
| `/api/agent` | `agent` | Agent 循环，通过 `app.emit()` 推送事件 |
| `/api/open` | `open_file` | 打开文件/文件夹 |

关键 Rust 依赖：
- `reqwest`：HTTP 请求（调用 DeepSeek API）
- `tokio`：异步运行时
- `tauri::Emitter`：向前端推送事件

### 5.4 前端迁移

- `fetch('/api/...')` → `invoke('command_name')`
- SSE 流 → Tauri 事件监听（`listen('agent-event', ...)`）
- 引入 `@tauri-apps/api` 包

### 5.5 静态导出

- 切换到 `@sveltejs/adapter-static`
- 添加 `+layout.js`：`export const prerender = true; export const ssr = false;`
- `npm run build` 输出到 `build/` 目录

### 5.6 打包结果

```
src-tauri/target/release/conversation-canvas.exe  (13.6MB)
```

- 不需要 Node.js 运行时
- 双击即用
- 前端静态文件内嵌在 exe 中

---

## Bug 修复记录

| Bug | 原因 | 修复 |
|-----|------|------|
| LiteGraph 内置按钮文字偏下 | `textBaseline` 和 `+1` 偏移 | 改用 Canvas 自定义绘制按钮 |
| Agent 节点间距太近 | `lastY` 从 promptNode 位置开始算 | 改为从 responseNode 底部开始 |
| AI 回复和 Agent 节点重叠 | 未考虑回复节点高度 | `lastY` 初始化为 `responseNode.pos[1] + responseNode.size[1]` |
| Tauri 编译失败 `Emitter` | Rust 2021 edition 不自动导入 trait | 添加 `use tauri::Emitter` |
| Tauri 打包 MSI 失败 | WiX 工具下载网络断开 | 暂用 .exe 分发 |
| Rust 安装慢 | 未设置代理 | 添加 `http_proxy` 环境变量 |

---

## v0.6.0 开发日志

### 2026-06-21 | Day 1 | 安全 P0 + 体验 P0 修复

**任务来源**：综合审计报告"第一周：安全 P0 + 体验 P0"

#### 任务 1：API Key 不泄露到导出 JSON

**问题**：导出对话 JSON 时，`_apiKey` 字段被序列化到文件中，用户分享对话时会泄露 API Key。

**修复**：
- 文件：`src/lib/canvas/io.js:15`
- 改动：删除 `if (node._apiKey) extra._apiKey = node._apiKey;` 这一行
- 添加注释说明 API Key 不导出的原因

**影响**：导出的 JSON 文件不再包含 API Key，防止敏感信息泄露。

#### 任务 2：搜索变量名错误修复

**问题**：`focusNode` 函数中引用了 `canvasElement`，但实际绑定的变量名是 `canvasEl`，导致搜索功能不可用。

**修复**：
- 文件：`src/routes/+page.svelte:204-205`
- 改动：将 `canvasElement` 替换为 `canvasEl`
- 删除多余的 `$effect(() => { canvasElement = canvasEl; })` 同步代码（第 238-240 行）

**影响**：搜索功能现在可以正常定位到搜索结果的节点。

#### 任务 3：Agent 执行中禁用发送按钮

**问题**：用户可以在 Agent 执行过程中重复点击发送按钮，导致多个 Agent 循环并发执行。

**修复**：
- 文件：`src/lib/canvas/PromptNode.js:9,18-21`
  - 添加 `_isRunning` 状态属性
  - 修改按钮回调，当 `_isRunning` 为 true 时不执行
  
- 文件：`src/lib/canvas/agent.js:22,142-143`
  - 在 Agent 开始时设置 `promptNode._isRunning = true`
  - 在 finally 块中恢复 `promptNode._isRunning = false`
  
- 文件：`src/routes/+page.svelte:17,178-190`
  - 添加 `agentRunning` 状态变量
  - 在 bindSend 函数中管理 `agentRunning` 状态

**影响**：Agent 执行期间，发送按钮被禁用，防止重复发送导致并发问题。

---

### 2026-06-21 | Day 2 | 接入 SetupDialog + 配置持久化

**任务来源**：综合审计报告"第一周：安全 P0 + 体验 P0"

#### 任务 4：接入 SetupDialog + 配置持久化

**问题**：SetupDialog 组件已经开发完成，但未接入到主页面。用户配置无法持久化，刷新页面即丢失。

**修复**：

**Rust 后端（已完成，无需修改）**：
- 文件：`src-tauri/src/lib.rs:730-769`
- 功能：
  - `AppConfig` 结构体定义配置字段
  - `save_config` 命令：将配置保存到 `%AppData%/conversation-canvas/config.json`
  - `load_config` 命令：从配置文件加载已保存的配置

**前端接入**：
- 文件：`src/routes/+page.svelte:103-135`
  - 修改 `onMount` 函数，添加配置加载逻辑
  - 启动时调用 `invoke('load_config')` 加载已保存的配置
  - 如果配置存在且 `remember` 为 true，应用配置
  - 如果没有配置，显示 SetupDialog（`showSetup = true`）
  
- 文件：`src/routes/+page.svelte:339-341`
  - 在模板中添加 SetupDialog 渲染：`{#if showSetup} <SetupDialog onSetup={onSetupComplete} /> {/if}`

**配置流程**：
1. 用户首次启动应用 → 显示引导 → 显示 SetupDialog
2. 用户填写配置 → 点击"保存并进入" → 调用 Rust `save_config` 保存到磁盘
3. 用户下次启动 → `load_config` 加载配置 → 自动应用，不再显示 SetupDialog
4. 如果用户选择"跳过"，不保存配置，下次启动仍会显示 SetupDialog

**影响**：
- 用户配置现在可以持久化存储
- 首次使用体验大幅提升，不再需要每次手动输入 API Key
- 配置界面美观，支持多种 AI 模型预设

---

### 2026-06-21 | Day 2 | 代码审查问题修复

**任务来源**：代码审查报告发现的遗留问题

#### 任务 5：双击画布空白处无法打开搜索节点

**问题**：`init.js` 中 `canvas.allow_searchbox = false` 禁用了 LiteGraph 内置的双击搜索节点功能，但工具栏提示"双击空白处搜索节点"，功能无法使用。

**修复**：
- 文件：`src/lib/canvas/init.js:58`
- 改动：将 `canvas.allow_searchbox = false` 改为 `canvas.allow_searchbox = true`

**影响**：用户双击画布空白处可以正常打开 LiteGraph 节点搜索框，用于快速添加新节点。

#### 任务 6：Agent 执行中发送锁定不完整

**问题**：`agentRunning` 状态变量虽然在发送前后正确设置，但 `bindSend` 回调没有在开头检查该状态，用户仍可通过重复点击触发多个并发 Agent 任务。

**修复**：
- 文件：`src/routes/+page.svelte:189`
- 改动：在 `bindSend` 的 `sendCallback` 回调开头添加 `if (agentRunning) return;` 检查

**影响**：Agent 执行期间，任何重复的发送操作都会被直接忽略，彻底防止并发问题。

#### 任务 7：SetupDialog 文案误导

**问题**：SetupDialog 中"记住我的配置（加密存储到本地）"文案与实际行为不符，配置实际以明文 JSON 存储在 `%AppData%/conversation-canvas/config.json`。

**修复**：
- 文件：`src/lib/components/SetupDialog.svelte:78`
- 改动：将"加密存储到本地"改为"存储到本地"

**影响**：UI 文案与实际存储行为一致，不再误导用户以为配置已加密。

#### 任务 8：搜索节点列表清理

**问题**：双击画布空白处打开搜索框时，除了 7 个自定义节点外，还显示 LiteGraph 内置节点条目（math/operation、math/compare 等 searchbox_extras），干扰用户。

**修复**：
- 文件：`src/lib/canvas/init.js:48-51`
- 改动：在清除 `registered_node_types` 之后，额外清除 `LiteGraph.searchbox_extras`、`slot_types_default_in`、`slot_types_default_out`

**影响**：搜索框只显示项目自定义的 7 种节点，无内置节点干扰。

#### 任务 9：缩短双击打开搜索的时间间隔

**问题**：LiteGraph 内置双击间隔硬编码为 300ms，手感偏慢。

**修复**：
- 文件：`src/lib/canvas/init.js:86-95`
- 改动：添加原生 `dblclick` 事件监听器（捕获阶段），在空白区域双击时直接调用 `canvas.showSearchBox()`。浏览器原生 dblclick 间隔约 200ms，比 LiteGraph 的 300ms 更快。同时移除之前设置的 `allow_searchbox = false`（已在任务 5 中改为 true）

**影响**：双击空白处打开搜索节点的响应更快。

#### 任务 10：搜索框点击外部关闭

**问题**：双击打开搜索框后，如果是误触，无法通过点击外部关闭。LiteGraph 默认只支持 ESC 键关闭，且搜索框的 blur 处理器会无条件 refocus，阻止了点击外部关闭。

**修复**：
- 文件：`src/lib/canvas/init.js:97-109`
- 改动：
  - 添加捕获阶段的 `pointerdown` 监听器（先于 LiteGraph 的 `stopPropagation`）
  - 检测点击目标是否在搜索框外部，如果是则调用 `canvas.search_box.close()`
  - 关闭后临时禁用 `canvas.allow_searchbox`，防止 LiteGraph 的 `processMouseDown` 在同一事件中重新打开搜索框
  - 使用 `requestAnimationFrame` 在下一帧恢复 `allow_searchbox`

**影响**：用户可以通过点击搜索框外部（画布、工具栏等任意位置）关闭搜索框，无需记忆 ESC 快捷键。

---

### 2026-06-21 | Day 3 | 安全加固

**任务来源**：综合审计报告"第一周：安全 P0 + 体验 P0"

#### 任务 11：execute_command 安全防护

**问题**：`execute_command` 工具没有安全防护，LLM 可以执行任意系统命令（如 `rm -rf /`、`format`、`regedit` 等），等同于给 LLM 一个 root shell。

**修复**：
- 文件：`src-tauri/src/lib.rs:10-55, 316-348`
- 改动：

  **1. 添加命令黑名单**（第 10-55 行）：
  - 定义 `DANGEROUS_COMMANDS` 常量，包含 20+ 种危险命令模式
  - 涵盖：文件系统破坏（rm -rf, format）、系统破坏（shutdown, regedit）、权限提升（net user）、网络攻击（curl|sh）等
  - 实现 `is_dangerous_command()` 函数，支持简单的通配符匹配

  **2. 添加 30 秒超时**（第 56 行）：
  - 定义 `COMMAND_TIMEOUT_SECS` 常量为 30 秒
  - 使用 `tokio::time::timeout` 包装命令执行
  - 超时后返回明确的超时错误信息

  **3. 修改 execute_command 实现**（第 316-348 行）：
  - 执行前调用 `is_dangerous_command()` 检查
  - 使用 `tokio::process::Command` 替代 `std::process::Command` 以支持异步超时
  - 返回详细的错误信息（命令内容、超时时间等）

**影响**：
- 危险命令会被自动拦截，返回"安全拒绝"错误
- 长时间运行的命令会在 30 秒后超时终止
- Agent 模式下 LLM 无法执行破坏性操作

#### 任务 12：search_in_files 参数化调用

**问题**：`search_in_files` 使用字符串拼接构造 `findstr` 命令，`query` 参数如果包含引号 `"` 可以闭合引号，注入任意命令。

**修复**：
- 文件：`src-tauri/src/lib.rs:352-370`
- 改动：
  - 移除字符串拼接的命令构造方式
  - 使用参数化调用，将 `query` 作为独立参数传递给 `findstr`
  - 构建搜索路径列表（支持多种文件扩展名）
  - 使用 `cmd_args.push()` 逐个添加参数，避免注入

**影响**：
- 消除了命令注入漏洞
- `query` 参数中的特殊字符（引号、括号等）不再影响命令执行
- 搜索功能更安全可靠

---

### 2026-06-21 | Day 4-5 | 体验 P0 修复

**任务来源**：综合审计报告"第一周：安全 P0 + 体验 P0"

#### 任务 13：替换 prompt() 为应用风格模态框

**问题**：文本输入使用浏览器原生 `prompt()` 对话框，与深色主题不搭，不支持多行编辑，用户体验差。

**修复**：

**新建文件**：

1. `src/lib/components/TextEditModal.svelte`
   - 应用风格的文本编辑模态框组件
   - 支持单行输入和多行 textarea
   - 深色主题设计，与应用整体风格一致
   - 支持 ESC 快捷键取消、Enter 确认（单行模式）
   - 自动聚焦、点击外部关闭

2. `src/lib/services/textEditService.js`
   - 全局文本编辑服务
   - 提供 `showTextEdit()` Promise API
   - 允许从非 Svelte 代码（widgets.js）触发模态框
   - 管理模态框状态和回调

**修改文件**：

3. `src/routes/+page.svelte`
   - 导入 TextEditModal 组件和 textEditService
   - 添加 `textEditState` 状态变量
   - 初始化文本编辑服务回调
   - 在模板中渲染 TextEditModal

4. `src/lib/canvas/init.js` ← **关键修复**
   - 导入 `showTextEdit` 函数
   - 创建 canvas 后覆盖 `canvas.prompt` 方法
   - LiteGraph 内置 `'string'` widget 点击时会调用 `canvas.prompt()`
   - 覆盖此方法可一次性拦截所有 4 处 `addWidget('string')` 调用（PromptNode + ModelConfigNode）

**技术方案**：
- 使用 Promise-based API 替代同步的原生 prompt
- 全局服务模式：覆盖 `canvas.prompt` → 调用 `showTextEdit()` → 模态框显示 → 用户输入 → Promise resolve
- LiteGraph string widget 内部调用 `canvas.prompt(title, value, callback, event, multiline)`，覆盖后所有 string widget 自动生效

**效果**：
- 文本编辑现在使用应用风格的深色主题模态框
- 支持多行文本编辑（System Prompt 等场景）
- 快捷键支持：ESC 取消、Enter 确认
- 整体 UI 风格统一，不再有原生弹框的违和感

---

## 技术债务

| 项目 | 说明 | 影响 |
|------|------|------|
| LiteGraph.js 停更 | 最后更新 2023，npm 原版功能有限 | 长期维护风险 |
| API Key 明文存储 | 存在前端内存中 | 安全风险，需系统钥匙串 |
| 无 MSI 安装包 | WiX 下载失败 | 分发不够用户友好 |
| 无自动更新 | 用户需手动下载新版本 | 体验不佳 |
| 无 TypeScript | 全部 JS，类型不安全 | 维护成本高 |
